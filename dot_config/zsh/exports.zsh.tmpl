# Environment exports
# Managed by chezmoi from .chezmoidata.yaml

export EDITOR="{{ .development.editor }}"
export VISUAL="{{ .development.editor }}"

# SSH agent
{{ if ne .chezmoi.os "darwin" -}}
export SSH_AUTH_SOCK="$XDG_RUNTIME_DIR/ssh-agent.socket"
{{ end -}}
